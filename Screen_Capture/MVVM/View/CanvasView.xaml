<UserControl x:Class="Screen_Capture.MVVM.View.CanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Screen_Capture.MVVM.View"
             xmlns:beh="clr-namespace:Screen_Capture.Core"
              xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Canvas x:Name="canvas"  >
        <Canvas.Background>
            <ImageBrush ImageSource="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.Image}"></ImageBrush>
        </Canvas.Background>
        <i:Interaction.Behaviors>
            <beh:EventToCommandBehavior Event="MouseLeftButtonDown"  Command="{Binding Cvs_MouseLeftButtonDown }" PassArguments="True"></beh:EventToCommandBehavior>
            <beh:EventToCommandBehavior Event="MouseMove"  Command="{Binding Cvs_MouseMove }" PassArguments="True"></beh:EventToCommandBehavior>
            <beh:EventToCommandBehavior Event="MouseLeftButtonUp"  Command="{Binding Cvs_MouseLeftButtonUp }" PassArguments="True"></beh:EventToCommandBehavior>
        </i:Interaction.Behaviors>
    </Canvas>
</UserControl>
